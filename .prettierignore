dist
pnpm-lock.yaml

# 전신 memorize의 SoT 계승 사본 (규격 아님) — 원본과 바이트 단위 대조 가능해야 하므로
# 포맷터로 재배치하지 않는다. 앞으로 추가되는 계승 문서도 같은 이유로 여기 덮인다.
docs/inherited/

# #405: FileLlmCallCacheStore.set()이 그대로 써내는 LLM 캐시 픽스처 — `pnpm bench:pr-smoke:record`로
# 재생성될 때마다 프리티어 포맷을 다시 맞춰야 하는 부담을 만들지 않도록, 머신이 쓴 그대로 커밋한다.
packages/mori/src/bench/preference-regression/pr-smoke-fixtures/

# #470: mori retrieval이 실제로 주입한 원문을 그대로 인용하는 감사 문서 — 중첩된 ```ts 코드
# 펜스 안의 탭 인덴트 자체가 관측 대상이라, 프리티어가 embeddedLanguageFormatting으로 그
# 탭을 스페이스로 재포맷하면 인용이 오염된다. 원문 바이트 그대로 유지한다.
docs/bench/injection-content-audit-2026-08-15.md
