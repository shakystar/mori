# 테스트 컨벤션

이 문서는 mori 리포에 테스트를 추가할 때 따르는 기준이다. 에이전트가 매 세션 이 문서를
참조하고, 여기 없는 판단은 이슈의 수용 기준을 우선한다.

## 단위 테스트 vs 통합 테스트

- **단위 테스트**: 하나의 모듈(함수/클래스)이 공개 인터페이스를 통해 약속한 동작 하나를
  검증한다. 프로세스 경계를 넘지 않는다.
- **통합 테스트**: 여러 모듈이 실제로 맞물릴 때만 드러나는 동작을 검증한다 — 파일시스템
  원자성(`packages/kernel/tests/integration/append-atomicity.test.ts`), 커넥션을 닫았다
  열어도 디스크 파일이 살아남는지(`event-store-roundtrip.test.ts`) 같은, 단위 테스트로는
  재현되지 않는 크로스 컴포넌트 시나리오.

두 종류 모두 실제 파일시스템/SQLite를 `mkdtemp`로 만든 임시 디렉터리에서 쓴다
(`node:fs`나 DB 드라이버를 목(mock)으로 대체하지 않는다). 목으로 흉내 낸 실행 경로는 실제
코드 경로와 어긋날 수 있고, 그 어긋남 자체가 버그를 가려버리기 때문이다. 프로세스를 새로
띄우거나(예: CLI 바이너리 spawn), 네트워크 I/O가 필요하거나, 셋업이 여러 모듈에 걸치면
통합 테스트 쪽으로 분류한다.

### 예외: 타이밍 레이스·장애 주입

원칙은 위와 같지만, 검증 대상이 **실제 I/O 타이밍에 의존하는 레이스나 스케줄러가 자연히
만들어내지 않는 장애(디스크 풀 등)** 라서 목 없이는 결정론적으로 재현할 수 없는 경우가 있다.
이때는 `vi.mock`으로 해당 I/O 호출을 감싸 지연·실패를 주입하는 것을 예외로 허용한다 — 단
아래 조건을 모두 만족해야 한다:

- `importOriginal`로 실제 구현을 가져와 스프레드하고, 명시적으로 무장(arm)한 호출에만
  개입한다 (그 외 호출은 실제 구현 그대로 통과).
- assert 대상은 **관찰 가능한 최종 상태**다 (예: 프로젝션에 남은 레코드, 락 디렉터리
  존재 여부, 두 임계구역이 겹쳤는지). 목 자체의 호출 횟수·호출 순서를 assert하지 않는다 —
  그건 여전히 금지 패턴("구현 세부에 결합된 테스트")에 해당한다.
- 파일 상단 주석에 왜 목 없이는 재현 불가능한지(레이스의 성격, 대안이 실패하는 이유)를
  남긴다.

어느 파일이 현재 이 예외에 해당하는지는 이 문서에 목록으로 박아두지 않는다 — 파일 목록은
PR이 파일을 늘릴 때마다 사람이 손으로 동기화해야 하는 상태 사본이고, 그 사본이 코드와
어긋난 것 자체가 이 이슈(#184)의 발단이었다. 정본은 다음 명령이다:

```
grep -rn "vi\.mock" --include="*.test.ts" packages
```

이 명령이 찾아낸 각 파일이 위 세 조건(부분 목킹·관찰 가능한 상태만 assert·사유 주석)을
만족하는지가 심사 대상이다. 새로 예외를 추가하는 PR은 파일 상단에 사유 주석을 남기는 것으로
충분하고, 이 문서를 함께 고칠 필요는 없다.

## 파일 배치 규칙

리포에는 현재 패키지별로 서로 다른 배치 관례가 이미 자리잡혀 있다 (모순이므로 아래처럼
현행 기준으로 못박는다):

- **`packages/kernel`**: 테스트를 소스와 분리해 `tests/unit/`, `tests/integration/`
  아래에 둔다 (예: `tests/unit/db.test.ts`, `tests/integration/append-atomicity.test.ts`).
  import는 `../../src/...` 상대 경로.
- **`packages/mori`**: 테스트를 대상 소스 파일 옆에 colocate한다 (예:
  `src/tools/edit-file.ts` ↔ `src/tools/edit-file.test.ts`).

**규칙**: 기존 파일을 수정할 때는 그 파일이 속한 패키지의 기존 배치를 따른다. 그 패키지에
없던 새 하위 모듈을 추가할 때도 마찬가지로 같은 패키지 안의 기존 패턴을 그대로 따른다
(kernel에 추가하면 `tests/unit/` 또는 `tests/integration/`, mori에 추가하면 colocate).
두 패키지 중 하나를 골라야 하는 새 패키지를 만드는 경우에는 kernel 방식
(`tests/unit/` / `tests/integration/` 분리)을 기본값으로 채택한다 — 통합 테스트가
늘어났을 때 nightly로 분리(CI 예산 항목 참고)하기 쉬워지기 때문이다. 패키지 하나 안에서
두 관례를 섞지 않는다.

파일명은 대상 파일명 + `.test.ts`. 테스트 대상이 명확히 하나가 아니면(여러 모듈에 걸친
통합 테스트) 시나리오를 설명하는 이름을 쓴다 (예: `append-atomicity.test.ts`).

## 원칙

- **동작(behavior) 하나당 테스트 하나.** `it(...)` 설명은 "무엇을 하면 무엇이 된다"를
  한 문장으로 말할 수 있어야 한다. 여러 동작을 한 `it`에 우겨넣지 않는다.
- **공개 인터페이스 기준으로 검증한다.** exported 함수·CLI 출력·저장된 파일 내용처럼
  호출자가 실제로 관찰할 수 있는 것만 assert한다. private 함수를 테스트하려고 export를
  늘리지 않는다.
- 셋업/해제는 `beforeEach`/`afterEach`로 임시 디렉터리를 만들고 지우는 패턴을 따른다
  (`mkdtempSync(join(tmpdir(), '<패키지>-<시나리오>-'))` → `afterEach`에서 `rm`).

## 금지 패턴

- **구현 세부에 결합된 테스트**: 내부 상태·private 필드·호출 순서(spy on internal calls)를
  검증하는 테스트. 리팩터링만으로 깨지면 안 된다.
- **의미 없는 스냅샷**: 사람이 diff를 보고 옳고 그름을 판단할 수 없는 스냅샷 테스트
  (대형 객체 전체 dump 등). assert는 관련 있는 필드만 좁혀서 한다.
- **기존 케이스의 사소한 변형 중복**: 이미 있는 테스트와 입력값 하나만 다르고 같은 코드
  경로·같은 실패 모드를 확인하는 테스트. 새 동작이나 새 경계 조건이 아니면 추가하지 않는다.
- **커버리지 숫자를 맞추기 위한 테스트**: 어떤 동작도 실제로 보증하지 않으면서 라인만
  실행시키는 테스트.

## 테스트를 추가할지 판단하는 기준

1. 처리 중인 이슈의 수용 기준(acceptance criteria)에 테스트가 명시돼 있으면 그것부터 쓴다.
2. 그 외 추가는 최소한으로 한다 — 새로 만든 공개 동작 하나당 정상 경로 1개 + 의미 있는
   실패/경계 경로 정도. "혹시 몰라서" 추가하는 테스트는 지양한다.
3. 버그를 고쳤다면 그 버그를 재현하는 회귀 테스트 하나는 예외적으로 추가한다 (수용 기준에
   없어도).

## CI 예산과의 관계

PR 게이트 워크플로(`.github/workflows/ci.yml`의 `build-and-test`) 전체가 ~2분 예산을
유지한다(#47, #184에서 checkout·install·build 등 고정비를 감안해 재정의). `pnpm test`
자체는 이보다 훨씬 짧다. 무거운 통합 테스트(디스크 I/O가 크거나 프로세스를 여러 번 띄우는
것)가 늘어나 이 예산을 위협하면 nightly 워크플로로 분리할 예정이다 — 그 전까지는 통합
테스트도 워크플로 예산 안에 들도록 가볍게 유지한다(테스트당 디스크 I/O를 최소화하고,
불필요하게 큰 픽스처를 만들지 않는다).

`pnpm test`는 #185부터 turbo 경유다(`turbo run test`). 명령 이름과 통과/실패 판정은
그대로지만 두 가지가 달라졌다:

- **패키지마다 vitest가 따로 돈다** (`packages/kernel`, `packages/mori`). 한 실행에 묶여
  있지 않으므로, 두 패키지의 테스트가 같은 전역 자원(고정 경로의 디렉터리·DB 등)을 잡으면
  이전에는 없던 경합이 생긴다. 테스트는 계속 임시 디렉터리 등 **자기 몫의 자원만** 써야 한다.
- **변경되지 않은 패키지의 test는 캐시 히트로 스킵된다.** 캐시 키는 그 패키지의 git 추적
  파일 전부 + `tsconfig.base.json` 등 전역 의존이라, 테스트 파일 한 줄만 고쳐도 다시 돈다.
  패키지 **밖**에 새 입력(공유 픽스처 디렉터리 등)을 두면 이 키에 안 잡혀 stale 캐시가
  false-green을 만든다 — 그런 것이 필요하면 `turbo.json`의 `globalDependencies`에 함께 넣는다.
