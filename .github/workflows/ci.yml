name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.30.3

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.23.1
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Format check
        run: pnpm format:check

      - name: Build
        run: pnpm build

      - name: Typecheck tests
        run: pnpm typecheck:test

      # Runs vitest directly instead of `pnpm test` so the build above isn't
      # redone via the `pretest` lifecycle hook (that hook exists for `pnpm test`
      # to be self-sufficient in local dev, but CI already built in the step above).
      - name: Test
        run: pnpm exec vitest run

      # .github/scripts/*.sh는 bash 스크립트라 vitest가 다루지 않는다. 셸로 돌리는 이유는
      # 검증 대상이 bash의 errexit 의미론과 빈 출력 처리이기 때문이다 (#112).
      - name: CI script tests
        run: pnpm test:ci-scripts
