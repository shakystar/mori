# 기여 가이드

## 개발 종료 상태

이 프로젝트는 개발을 종료했다. 신규 기능·정기 유지보수 및 일반적인 외부 PR 수용은 계획하지 않는다.
MIT 조건에 따라 포크하여 사용할 수 있다. 종료 정리 변경은 별도 PR로 검토한다.
현재 구현·검증 범위는 [최종 상태](docs/final-status.md)에 있다.

아래 내용은 개발 당시의 협업·검토 규약을 보존한 것이다.
에이전트 구성 설명을 현재 플릿이 실행 중이라는 상태 보증으로 읽지 않는다.

이 리포는 **자율 에이전트 플릿**이 개발한다. 이슈가 작업 큐이고, 라벨이 상태 머신이다.
사람은 방향을 정하고 결정이 필요한 지점에만 개입한다.

에이전트 구성과 배포는 [shakystar/autopilot-agents](https://github.com/shakystar/autopilot-agents)에 있다.

## 누가 무엇을 하는가

| 주체          | 하는 일                                                                                                           | 모델                          |
| ------------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------- |
| **secretary** | Slack에서 사람과 대화하며 이슈 등록·결정 중계·상태 브리핑. 사람이 지목한 PR 머지 대행. 6시간마다 보드 불변식 점검 | fable-5                       |
| **owner**     | `idea`를 수용 기준이 있는 작업으로 분해. PR 리뷰·판정. blocked/failed 처리                                        | opus                          |
| **developer** | 작업 구현 → 테스트 → PR. worktree 격리, 브랜치 `agent/issue-N`                                                    | sonnet (`agent:opus` 시 opus) |
| **사람**      | 방향 결정, `agent:decision` 응답, 조건 미달 PR 머지                                                               | —                             |

세 에이전트가 같은 GitHub 계정을 쓰므로, **모든 산출물은 첫 줄이 `🤖 [에이전트명]` 서명으로 시작한다.**
서명이 없는 이슈·코멘트·PR은 사람이 쓴 것이다.

## 라벨 상태 머신

```
idea ──owner 분해──▶ agent:todo ──developer 착수──▶ agent:doing
                          ▲                              │
                          │                         PR 제출
                     수정요청                             ▼
                          │                        agent:review
                          └──────owner 판정───────────┤
                                                     승인
                                                      ▼
                                              agent:approved ──▶ 머지
```

곁길:

| 라벨             | 뜻                                | 누가 푸는가                                      |
| ---------------- | --------------------------------- | ------------------------------------------------ |
| `agent:blocked`  | 선행 작업 대기 또는 요구사항 질문 | owner (해소 감지 시 자동, 6시간마다 강제 재점검) |
| `agent:decision` | **사람 결정 대기**                | 사람 (Slack 또는 이슈 코멘트)                    |
| `agent:failed`   | 세션이 비정상 종료                | owner (산출물 확인 후 재시도, 2회 상한)          |
| `agent:opus`     | 이 작업은 opus로 실행             | 붙이는 사람/owner                                |

`agent:doing`은 어느 시점에도 **최대 1건**이다 (워커가 레포당 1대). 2건 이상이면 상태가 깨진 것이고
secretary 감시자가 Slack으로 보고한다.

라벨 전이는 각 에이전트의 하네스(run.sh)가 산출물(PR·판정 코멘트·마커 코멘트)을 관찰해 수행한다 —
에이전트 세션(LLM)은 라벨을 직접 조작하지 않는다 (autopilot-agents#4 Phase 2).

## 이슈 쓰는 법

New issue를 누르면 두 가지 템플릿이 뜬다. **크기로 고른다.**

- **작업 (`agent:todo`)** — developer가 한 세션(수십 turn)에 끝낼 수 있고, 수용 기준을 지금 쓸 수 있는 것
- **아이디어 (`idea`)** — 방향성·에픽급. owner가 분해한다

`gh issue create`로 만들면 템플릿이 자동으로 뜨지 않는다. 그때도 아래 형식을 지킨다.

### 완료 조건은 기계적으로 검증 가능해야 한다

이게 가장 중요하다. owner는 PR을 **수용 기준과 대조해서** 판정하므로, 판정할 수 없는 기준은
리뷰를 무의미하게 만든다.

| 나쁜 예                  | 좋은 예                                                                        |
| ------------------------ | ------------------------------------------------------------------------------ |
| 임베딩 의존성을 정리한다 | `packages/kernel` 트리에서 `fetch` 참조 0건 — grep 결과 PR 본문 첨부           |
| 테스트를 잘 정리한다     | 삭제한 테스트마다 "어느 테스트가 같은 코드경로를 덮는지" 근거를 PR 본문에 기록 |
| 동작이 깨지지 않는다     | 임베딩 미주입 시 FTS 전용 폴백 — 기존 테스트 그린으로 입증                     |

### 비범위를 반드시 적는다

에이전트는 "같은 부류니까 이것도" 하는 확장을 한다. **하지 말아야 할 것을 적지 않으면 범위가 번진다.**
왜 제외했는지와 언제 재론할지를 함께 쓰면 나중에 판단을 복원할 수 있다.

### 의존성과 순서를 적는다

같은 파일을 만지는 다른 이슈가 있으면 명시한다. owner는 자동 머지를 걸기 전에 **열린 PR 간
변경 파일 교집합**을 확인하는데, 이슈 본문에 순서가 적혀 있으면 그 판단이 정확해진다.

의존이 있는데 아직 안 풀렸다면 `agent:blocked`로 등록하고 해제 조건을 체크박스로 적는다 —
owner가 그 조건을 감지해 자동으로 `agent:todo`로 되돌린다.

### 크기 판단

한 세션에 못 끝낼 것 같으면 셋 중 하나를 고른다:

1. **직접 쪼갠다** — 각 조각이 독립적으로 빌드·테스트를 통과할 수 있을 때
2. **`idea`로 올린다** — 쪼개는 기준 자체를 owner에게 맡길 때
3. **`agent:opus`를 붙인다** — 쪼개면 중간 상태가 깨지는 원자적 변경일 때
   (예: seam 역전 — 인터페이스만 옮기고 구현을 안 옮기면 빌드가 깨진다)

`agent:opus`는 구독 한도를 크게 소모하므로 남발하지 않는다. owner는 기획 패스당 최대 1건만 붙인다.

## PR 규칙

- **기본 브랜치 직접 push 금지.** 모든 변경은 PR로.
- required status check `build-and-test`가 통과해야 머지된다.
- 충돌은 **merge로만** 해소한다. rebase와 force push는 금지 —
  이미 머지된 다른 작업을 버리게 된다.
- PR 본문에는 무엇을 왜 바꿨는지, 테스트 결과(실행한 명령과 출력 요약), 닫기 키워드를 포함한다.
  닫기 키워드는 **PR 본문 마지막 줄에 백틱 없이 단독으로** 쓴다: `Closes #N`.
  위 표기는 문서 안에서 읽기 좋으라고 백틱으로 감싼 것뿐이다 — **이 표기를 그대로 PR 본문에
  복사하면 안 된다.** 백틱 안에 있으면 GitHub이 닫기 키워드로 파싱하지 않아 PR이 머지돼도
  이슈가 열린 채 남는다 (실제 사고: `mori-nest` #82 / PR #86 — 이슈가 26시간 넘게
  `agent:review`에 고아로 남았다).
- 이슈 본문이 "판단과 근거를 기록하라"고 했다면 **그 판단을 PR 본문에 적는다.**

### 머지 권한

owner는 **세 조건을 모두 만족한 PR에만** `gh pr merge --auto --squash`를 건다:

1. owner 승인 (`✅ [owner 승인]` 코멘트 + 이슈 `agent:approved`)
2. CI 그린
3. 다른 열린 PR과의 변경 파일 교집합 0

`--auto`는 예약이 아니다 — 조건이 이미 충족돼 있으면 **즉시 머지된다.**
조건 미달 건(충돌, 파일 교집합)은 사람이 판단해서 머지한다.

owner가 `gh pr review`를 쓰지 않고 코멘트로 판정하는 이유는, owner와 developer가 같은 GitHub
계정이라 자기 PR에 대한 approve/request-changes가 거부되기 때문이다.

#### 낡은 그린 체크 재검증 (`recheck-open-prs.yml`)

`build-and-test`의 그린은 **그 PR이 마지막으로 갱신된 시점의 `main`** 에서 얻은 결과다.
브랜치 보호가 `strict: false`라 `main`이 앞서 나가도 다시 돌지 않고, 텍스트 충돌이 없는
시맨틱 충돌(시그니처 변경 등)은 아무 관문에도 걸리지 않는다. `main` push마다 도는
`recheck-open-prs.yml`이 이 구멍을 메운다 — 열린 PR을 러너에서 현재 `main`과 직접 머지해
`lint`/`format:check`/`build`/`typecheck:test`/`vitest run`을 돌리고, 레드면 **그 PR에
코멘트를 남긴다**(코멘트 하나를 계속 갱신한다). 드래프트와 충돌 PR은 건너뛴다.

**여전히 사람 몫인 것:** 이 워크플로는 required status check가 아니다. 레드로 판정돼도
GitHub은 그 PR을 여전히 머지 가능으로 보고하고 `--auto`도 막지 않는다. 그래서 owner는
머지 전에 **이 PR에 `🤖 [ci]` 재검증 코멘트가 레드로 붙어 있지 않은지** 확인한다.
코멘트가 없거나 그린이면 통과다. 재검증 결과가 아직 없는 갓 만든 PR은 종전처럼
CI run 시각과 `main` HEAD 시각을 대조한다.

**판정 코멘트는 하나다.** 위의 "재검증 코멘트"는 `<!-- recheck-open-prs -->` 마커가 붙은
코멘트 하나를 가리키고, 그 코멘트만 계속 갱신된다. 판정 종류는 본문에
`<!-- recheck-verdict:<green|red|conflict|stale|error> -->`로도 심긴다.

이와 별개로 `<!-- recheck-open-prs-notice -->` 마커가 붙은 **알림 코멘트**가 함께 보일 수
있다. 대상 선정(`discover`) 잡이 실패한 실행은 어떤 head·base로 검증할지조차 정하지 못한
채 돌기 때문에 **판정하지 않으며**, 이미 있는 판정을 덮지 않고 이 알림만 남긴다. 읽는 법은
바뀌지 않는다 — **머지 판단의 근거는 여전히 판정 코멘트 하나**이고, 알림 코멘트는 그린도
레드도 아니다. 알림이 뜻하는 것은 "판정 코멘트의 판정이 이 실행보다 앞선 실행의 것"뿐이다.
따라서 판정 코멘트가 레드·판정 불가면 종전대로 머지하지 않고, 그린이더라도 그 그린은 그
알림을 남긴 실행이 확인한 것이 아니므로 재검증을 다시 돌리거나 PR head를 갱신해 최신
판정을 받은 뒤 판단한다.

#### 낡은 레드 체크 해소 (`refresh-armed-prs.yml`)

위가 낡은 **그린**을 다룬다면 이쪽은 낡은 **레드**를 다룬다. `build-and-test`는 PR head 커밋에
붙은 결론으로 평가되는데, `main`이 앞서 나가도 head SHA는 그대로라 그 결론도 그대로다.
그래서 **PR 자기 diff와 무관한 이유로(원인이 `main`에 있어서) 레드가 박힌 PR**은 스스로 할 수
있는 일이 없고, `--auto`가 걸려 있어도 조건 미충족으로 영원히 대기한다 (#195 — #190이 이
상태를 armed PR 7건에 동시에 만들었다).

`main` push마다 도는 `refresh-armed-prs.yml`이 이 구멍을 메운다 — **`--auto`가 걸려 있고 ·
충돌이 아니며 · 현재 `main`을 아직 포함하지 않은** 열린 PR의 head를
`PUT /pulls/{n}/update-branch`로 갱신한다. 그 갱신이 `synchronize`를 일으켜 기존 `ci.yml`이
현재 `main`을 포함한 head에서 새 `build-and-test`를 남기므로, **required check의 생산 주체는
`ci.yml` 하나로 유지된다.** 대상을 좁게 거는 이유는 매 push마다 열린 PR 전부의 CI를 다시
돌리지 않기 위해서다.

**이 잡은 App 설치 토큰을 쓴다.** 기본 `GITHUB_TOKEN`이 만든 `synchronize` run은
approval-required 상태로 생성돼 사람이 "Approve and run"을 눌러야 시작하기 때문이다.
시크릿(`MERGE_REFRESH_APP_ID` / `MERGE_REFRESH_APP_PRIVATE_KEY`)이 없으면 이 잡은 이유를
잡 요약에 남기고 **no-op**한다 — 그때는 종전대로 owner가 수동으로 head를 갱신하거나 머지한다.

수동 실행(`workflow_dispatch`)도 되지만 **"Use workflow from"에서 반드시 `main`을 고른다.**
다른 ref에서는 `github.sha`가 main의 head가 아니라 (1) 실행할 스크립트를 신뢰할 수 없는 ref에서
가져오고 (2) 뒤처짐을 엉뚱한 base로 재게 된다. 잡이 `main`에 고정돼 있어 그 실행은 갱신을
하지 않고 이유를 남기며 레드가 된다.

## 코드 주석의 크로스파일 줄 인용

`packages/` 안의 크로스파일 줄 인용은 **리포 루트 상대 경로**로 적는다 (확장자 포함, basename
금지) — 조각2 추출기(예정)의 ㉠c 해석 규칙과 같은 규칙이다. 리포 밖의 위치(다른 저장소·PR diff)는
줄 인용 형태로 쓰지 않는다 — 대상 파일이 이 리포에 없어 검증도 advisory 체크도 할 수 없다.
(배경: `docs/line-citation-staleness-adjudication.md` §Q5, #303/#321)

이 관례는 **지금부터 새로 쓰거나 고치는 인용**에 적용된다. `packages/`에 아직 남아 있는
basename 형태의 기존 인용은 이 관례가 이행되지 않은 잔여이며, 그것을 전수 정리하는 자리는
이 문서가 아니라 #303 §Q5 / 조각2(#322)다.

## `docs/` 문서의 시점 규율 — 판정 기록의 동결

바로 위 절이 `packages/` 코드 주석의 인용 **경로 형태**를 정한다면, 이 절은 `docs/` 문서의
**시점** 규율을 정한다 — 문서가 가리키는 코드가 움직였을 때 누가 무엇을 하는가. 답은
"아무것도 하지 않는다"이고, 아래가 그 근거와 형식이다.

배경: 이 리포의 산정 문서들은 첫머리에 "기준 커밋 = X, 이 문서의 모든 `파일:줄` 인용은 그
커밋에서 대조한 것이다"라는 **전칭 선언**을 관례처럼 달아 왔는데, 그 선언은 인용 유지보수
PR 한 번으로 조용히 거짓이 된다 — 대상 8개 문서 중 6개가 실제로 거짓이 됐다
(`docs/baseline-declaration-staleness-adjudication.md` §Q1·§Q3, #324). 뿌리는 **"줄번호 참조"와
"살아있는 문서 의무"의 결합**이다. 산정이 이슈 코멘트로만 남았다면 독자가 자연히 그 시점
기록으로 읽었을 텐데, `docs/`로 커밋되면서 "코드와 어긋나면 문서를 고친다"는 의무가 붙었고
그 의무가 인용 갱신 PR 계열(#312·#314·#316~#319)을 낳았다. docs 방식은 유지하되 **의무 쪽을
끊는다.**

### 1. `docs/`는 두 부류다 — 헤더에 어느 쪽인지 적는다

| 부류              | 무엇인가                                                                                  | 코드가 바뀌면                                   |
| ----------------- | ----------------------------------------------------------------------------------------- | ----------------------------------------------- |
| **판정 기록**     | 특정 이슈·PR의 질문을 그 시점 코드를 재서 답한 문서 (산정·판정·설계 결정). `docs/` 대다수 | 아무것도 하지 않는다. 낡으면 새 문서가 대체한다 |
| **살아있는 정본** | 코드가 지켜야 할 규칙·목록의 정본. 다른 문서·코드·리뷰가 "정본"으로 참조한다. 소수        | 문서를 고친다                                   |

**판정 기준은 질문 하나다: "6개월 뒤 코드가 바뀌었을 때 이 문서를 고쳐야 하는가?"**

- "고쳐야 한다" → **살아있는 정본.** 예: `docs/storage-boundary-secrets.md` — 저장 경계를
  넘어가면 안 되는 값의 정본이라, 금지 목록이 코드에서 바뀌면 이 문서도 따라 바뀌어야 한다.
- "그때는 이 문서를 고치는 게 아니라 새로 잰다" → **판정 기록.** 예:
  `docs/baseline-declaration-staleness-adjudication.md`(#324 산정),
  `docs/compaction-consolidation-boundary.md`(#7 첫 조각의 설계 기록).
- **애매하면 판정 기록이 기본값이다.** 살아있는 정본으로 두는 것은 유지보수 의무를 새로
  만드는 쪽이고, 그 의무를 질 사람이 정해져 있을 때만 정당하다.

**새 문서를 쓸 때 어느 부류인지 헤더에 적는다** — 제목(H1) 바로 아래, 아래 4의 블록으로.
부류 표시가 없는 문서는 판정 기록으로 읽는다.

`docs/inherited/` 아래는 이 분류 밖이다 — 전신 프로젝트 `memorize`의 기록이고 읽는 법은
`docs/inherited/_INHERITED.md`가 따로 정해 뒀다. 동결 헤더를 달지 않는다.

### 2. 판정 기록은 머지 시점에 동결한다

ADR의 불변 관례와 같다. 머지된 판정 기록은 **고치지 않는다.** 내용이 낡으면 그 문서를
갱신하는 것이 아니라 **새 문서가 대체(supersede)한다** — 새 문서 첫머리에 무엇을 대체하는지
적고 원 문서는 그대로 둔다.

- **금지: 동결 문서를 겨누는 인용 갱신 PR은 만들지 않는다.** 권장이 아니라 규율이다. 동결
  문서의 `파일:줄` 인용이 코드 변경으로 낡은 것은 **결함이 아니다** — 그 문서는 애초에 오늘의
  코드를 보증하지 않는다고 자기 헤더에 적고 있다. 이 계열의 일감이 통째로 사라지는 것이 이
  관례의 목적이다.
- **유일한 예외는 동결 헤더를 다는 PR 자신이다.** 그 PR은 헤더만 넣고 인용값은 손대지
  않는다 — 같은 PR에서 인용을 갱신하는 것이 이 관례가 없애려는 바로 그 행위다.
- **정정은 갱신이 아니다.** 오탈자·깨진 링크·서술 오류처럼 **그 문서가 쓰인 시점에 이미
  틀렸던 것**을 고치는 것은 계속 허용된다. 구분선은 "코드가 움직여서 낡았나"(고치지 않는다)
  대 "쓸 때부터 틀렸나"(고친다)다.
- **#322의 advisory 체크와의 관계: 동결 문서는 그 경고의 대상이 아니다.** 그 체크(#303
  조각2 = #322)는 `packages/` 아래 인용만 추출하고 `docs/`는
  **의도적으로 제외**한다 — 경고를 받은 PR이 남의 산정 문서를 고칠 권한이 없어 행동으로
  이어질 수 없기 때문이다(#303 §Q4.1). 이 절이 그 제외에 이유를 하나 더 준다: 동결 문서는
  낡는 것이 정상이므로 경고 대상이 되어서도 안 된다. `docs/`를 그 체크에 넣자는 제안이
  나오면 이 절을 근거로 각하한다.

### 3. 인용은 심볼 이름 우선, 줄번호에는 국소 SHA를 병기한다

`docs/` 문서가 코드를 가리킬 때:

1. **심볼 이름을 먼저 쓴다** — 함수명·타입명·절 제목. 줄이 밀려도 낡지 않는다. 선례는
   [mori-nest #38](https://github.com/shakystar/mori-nest/issues/38) 본문의 "인용은 심볼
   이름으로만 적는다. 값·줄번호는 옮겨 적지 말고 착수 시점 `main`에서 직접 열어라"다.
2. **줄번호가 꼭 필요하면 국소 SHA를 그 자리에 병기한다** —
   `<리포 루트 상대 경로>:<줄> (<그 줄을 대조한 short SHA>)` 꼴로, 문서 전체가 아니라 **그 인용
   하나가** 어느 커밋 기준인지를 옆에 적는다. 문서 첫머리의 **전칭 선언은 더 쓰지 않는다** —
   그것이 #324가 실측한 실패다.
3. **값·줄번호를 다른 문서에서 옮겨 적지 않는다.** 이슈 본문이나 앞선 산정에 적힌 `파일:줄`을
   그대로 베끼면 그 글이 쓰인 시점의 값을 옮기는 것이다. 착수 시점 `main`에서 직접 열어
   확인한 값만 적는다.

파일 경로 형태(리포 루트 상대, 확장자 포함)는 위 "코드 주석의 크로스파일 줄 인용" 절과 같은
규칙을 쓴다. 위 절은 **파일을 어떻게 적는가**를, 이 절은 **무엇을 가리키고 언제 기준인가**를
정한다.

### 4. 헤더 정본 문구 (그대로 복붙한다)

문구를 문서마다 새로 짓지 않는다. 세 문서에 세 가지 표현이 생기면 그것을 다시 통일하는
일감이 생긴다.

**판정 기록** — H1 바로 아래에 붙이고 `<커밋-SHA>` 자리만 채운다:

```markdown
> **판정 기록 · 동결됨 (커밋 `<커밋-SHA>` 시점).** 이 문서는 그 시점의 기록이며 오늘의 코드를
> 보증하지 않는다. **갱신하지 않는다** — 낡으면 새 문서가 대체(supersede)한다. 인용이 코드와
> 어긋나 보이면 이 문서가 아니라 코드를 따른다.
```

**살아있는 정본** — 같은 자리에 붙인다. SHA 자리가 없다:

```markdown
> **살아있는 정본.** 이 문서는 현재 코드에 대한 정본이며, 코드와 어긋나면 이 문서를 고친다.
> (부류 규율은 `CONTRIBUTING.md`의 "`docs/` 문서의 시점 규율" 절)
```

`<커밋-SHA>`에 무엇을 적는가 — **그 문서가 코드를 대조한 시점**이다:

- **새로 쓰는 문서**: 착수 시점 `main`의 short SHA (`git rev-parse --short origin/main`).
  머지 커밋 SHA가 아니다 — PR을 쓰는 시점에 알 수 없고, 문서가 실제로 읽은 트리도 그것이 아니다.
- **이미 머지된 문서에 나중에 헤더를 다는 경우**: 그 문서가 원래 선언했던 기준 커밋을 그대로
  옮긴다. 그런 선언이 없으면 그 문서가 머지된 커밋을 쓰고, 고른 근거를 PR 본문에 한 줄 적는다.

## 사람이 개입하는 지점

플릿은 기본적으로 자율 동작한다. 다음 세 가지만 사람 몫이다.

1. **`agent:decision`** — 아키텍처 방향, 되돌리기 어려운 갈림길, 이전 결정의 번복.
   owner가 선택지와 추천안을 코멘트로 정리해두면 결정만 하면 된다.
   Slack에서 secretary에게 말해도 되고 이슈에 직접 코멘트해도 된다.
2. **조건 미달 PR 머지** — 충돌이 있거나 다른 PR과 파일이 겹치는 경우.
3. **재시도 2회를 넘긴 `agent:failed`** — 기계가 두 번 시도해도 안 되는 것.

`agent:blocked`는 사람 몫이 아니다. owner가 해소를 감지해서 자동으로 푼다.
